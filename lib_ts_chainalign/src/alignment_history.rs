//! Data structures for tracking the most recent alignment history in order to exclude subalignments that could form a valid inexact anchor.

pub mod extension_graph;
pub mod history_alignment_operations;
pub mod history_vec;
